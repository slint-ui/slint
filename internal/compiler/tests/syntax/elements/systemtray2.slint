// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

//config:generate_all_exported_windows

export component A inherits SystemTray {
    Rectangle {}
//  >        <error{Rectangle is not allowed within SystemTray. Only Menu are valid children}
}

export component B inherits SystemTray {
    Text { text: "nope"; }
//  >   <error{Text is not allowed within SystemTray. Only Menu are valid children}
}

export component C inherits SystemTray {
    TouchArea {}
//  >        <error{TouchArea is not allowed within SystemTray. Only Menu are valid children}
}
