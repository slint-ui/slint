// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

// Issue #2902

import { HorizontalBox, VerticalBox } from "std-widgets.slint";

export component MainWindow inherits Window {

    HorizontalBox {
//  >            <note{setting an 'x' or a 'y' here takes 'HorizontalBox' out of its parent's size, one way out of the binding loop reported for 'width' at line 15}

        VerticalBox {

            width: parent.width;
//                 >           <warning{The binding for the property 'width' is part of a binding loop (root.layoutinfo-h -> HorizontalBox.width -> VerticalBox.width -> HorizontalBox.layoutinfo-h -> root.layoutinfo-h)↵Compute 'width' without depending on a size or a position the layout produces, or set an 'x' or a 'y' on 'HorizontalBox', so its parent stops sizing itself from it↵This was allowed in previous version of Slint, but is deprecated and may cause panic at runtime}
            //height: parent.height;
            Text {
                text: "Test";
            }
        }
    }
}
