// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

// `@deprecated` applies to callbacks and functions too, warning when they are used from outside
// the declaring component.

component Inner {
    callback new-callback();
    @deprecated("Use 'new-callback' instead") callback old-callback <=> new-callback;
    @deprecated("Use 'new-callback' instead") callback old-handler-callback();
    @deprecated("Use 'new-function' instead") public pure function old-function() -> int { 1 }
    public pure function new-function() -> int { 2 }

    // An empty message deprecates without advising on a replacement.
    @deprecated("") callback no-advice-callback();
    @deprecated("") public pure function no-advice-function() -> int { 3 }

    // No warning for a use within the declaring component:
    out property <int> internal: self.old-function();
}

export component Test {
    inner := Inner {
        // Setting a handler on a deprecated callback warns too:
        old-handler-callback => { }
//      >                  <warning{The callback 'old-handler-callback' has been deprecated. Use 'new-callback' instead}
    }

    callback trigger();
    trigger => {
        inner.old-callback();
//            >          <warning{The callback 'old-callback' has been deprecated. Use 'new-callback' instead}
    }

    out property <int> called: inner.old-function();
//                                   >          <warning{The function 'old-function' has been deprecated. Use 'new-function' instead}

    trigger2 => {
        inner.no-advice-callback();
//            >                <warning{The callback 'no-advice-callback' has been deprecated}
    }
    callback trigger2();
    out property <int> plain: inner.no-advice-function();
//                                  >                <warning{The function 'no-advice-function' has been deprecated}
}
