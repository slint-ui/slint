---
<!-- Copyright © SixtyFPS GmbH <info@slint.dev> ; SPDX-License-Identifier: MIT -->
title: Grid
description: Grid API.
---

import CodeSnippetMD from '/src/components/CodeSnippetMD.astro';
import SlintProperty from '/src/components/SlintProperty.astro';


The `Grid` layout is a grid layout that arranges its children in a grid.

It extends the `GridLayout` component, but replaces the padding,  spacing-vertical and spacing-horizontal properties with the `MaterialStyleMetrics` values.
