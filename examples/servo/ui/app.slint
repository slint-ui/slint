// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: MIT

import {
    VerticalBox,
    HorizontalBox,
    Button,
    LineEdit,
} from "std-widgets.slint";

import { Webview } from "../src/webview/webview.slint";
import { MobileMenuContent } from "mobile_menu.slint";

export { WebviewLogic } from "../src/webview/webview.slint";
export { Palette } from "std-widgets.slint";


export component MyApp inherits Window {
    preferred-width: 1224px;
    preferred-height: 768px;

    property <length> toolbar-height: 50px;
    property <length> toolbar-padding: 8px;
    property <length> toolbar-spacing: 8px;

    // On mobile (e.g. iOS/Android) safe-area-insets.top is non-zero.
    property <bool> is-mobile: root.safe-area-insets.top > 0px;

    menu := PopupWindow {
        x: root.width - self.width - root.safe-area-insets.right - root.toolbar-padding;
        y: root.safe-area-insets.top + root.toolbar-height;
        width: menu_content.width;
        height: menu_content.height;
        close-policy: close-on-click;

        menu_content := MobileMenuContent {
            back => {
                webview.back();
            }
            forward => {
                webview.forward();
            }
            reload => {
                webview.reload();
            }
            go => {
                webview.loadUrl(lineEdit.text);
            }
        }
    }

    VerticalBox {
        padding-left: root.safe-area-insets.left;
        padding-top: root.safe-area-insets.top;
        padding-right: root.safe-area-insets.right;
        padding-bottom: root.safe-area-insets.bottom;

        HorizontalBox {
            height: root.toolbar-height;
            padding: root.toolbar-padding;
            spacing: root.toolbar-spacing;

            if !root.is-mobile: Button {
                text: "Back";
                clicked => {
                    webview.back();
                }
            }

            if !root.is-mobile: Button {
                text: "Forward";
                clicked => {
                    webview.forward();
                }
            }

            if !root.is-mobile: Button {
                text: "Reload";
                clicked => {
                    webview.reload();
                }
            }

            lineEdit := LineEdit {
                text: webview.current_url;
                horizontal-stretch: 1;
                accepted(url) => {
                    webview.loadUrl(lineEdit.text);
                }
            }

            if !root.is-mobile: Button {
                text: "Go";
                clicked => {
                    webview.loadUrl(lineEdit.text);
                }
            }

            if root.is-mobile: Button {
                text: "...";
                clicked => {
                    menu.show();
                }
            }
        }

        Rectangle {
            webview := Webview {
                width: 100%;
                height: 100%;
                changed current_url => {
                    lineEdit.text = self.current_url;
                }
            }
        }
    }
}
