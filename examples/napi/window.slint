export component MainWindow inherits Window {
    width: 600px;
    height: 400px;
    title: "napi test";

    HorizontalLayout {
        padding: 8px;

        Text {
            color: black;
            text: "Hello from napi";
        }
    }
}