# Copyright © SixtyFPS GmbH <info@slint.dev>
# SPDX-License-Identifier: MIT

[project]
name = "system_tray"
version = "1.17.0"
description = "Slint SystemTrayIcon Python Example"
requires-python = ">=3.12"
dependencies = ["slint"]

[tool.uv.sources]
slint = { path = "../../api/python/slint", editable = true }
