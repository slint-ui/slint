build/

# Generated by build_runner from lib/ui/todo.slint; regenerate with
# `fvm dart run build_runner build --delete-conflicting-outputs`.
*.slint.dart

# The platform runner directories are generated, not committed. Recreate them
# with `flutter create --platforms=<platform> .` — see the README.
android/
ios/
linux/
macos/
web/
windows/
