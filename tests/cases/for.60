
TestCase := Rectangle {
    width: 100;
    height: 100;
    color: white;

   for pp[idx] in 5: Rectangle {
        Rectangle {
            x: 2 * idx;
            y: 200 * pp;
        }
    }

    for pp[idx] in [1,3,2]: Rectangle {
        x: idx;
        y: 25 * pp;
    }

    for pp[idx] in ["1","3","2"]: Rectangle {
        x: idx;
        Text { text: pp; }
    }
}
