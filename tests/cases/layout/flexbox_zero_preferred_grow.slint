// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

// `preferred-width: 0` with `alignment: stretch` (the closest analogue of the
// CSS `flex: 1 1 0` idiom) distributes the whole main size by the
// horizontal-stretch factors.

component ZeroPreferredStatic inherits Rectangle {
    width: 400px;
    height: 50px;
    FlexboxLayout {
        alignment: stretch;
        flex-wrap: no-wrap;
        r1 := Rectangle {
            horizontal-stretch: 1;
            preferred-width: 0px;
            height: 50px;
        }
        r2 := Rectangle {
            horizontal-stretch: 3;
            preferred-width: 0px;
            height: 50px;
        }
    }

    // Both grow from a zero base, so the whole 400px is split 1:3.
    out property <bool> test: r1.width == 100px && r2.width == 300px;
}

component ZeroPreferredRepeated inherits Rectangle {
    width: 400px;
    height: 50px;
    FlexboxLayout {
        alignment: stretch;
        flex-wrap: no-wrap;
        r1 := Rectangle {
            horizontal-stretch: 1;
            preferred-width: 0px;
            height: 50px;
        }
        for g in [1.0]: Rectangle {
            horizontal-stretch: g;
            preferred-width: 0px;
            height: 50px;
        }
    }

    out property <bool> test: r1.width == 200px;
}

export component TestCase inherits Window {
    width: 400px;
    height: 400px;

    VerticalLayout {
        t1 := ZeroPreferredStatic { }
        t2 := ZeroPreferredRepeated { }
    }

    out property <bool> test: t1.test && t2.test;
}

/*
```cpp
auto handle = TestCase::create();
assert(handle->get_test());
```

```rust
let instance = TestCase::new().unwrap();
assert!(instance.get_test());
```

```js
var instance = new slint.TestCase();
assert(instance.test);
```
*/
