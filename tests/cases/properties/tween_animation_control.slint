// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

export component TestCase inherits Rectangle {
    width: 200px;
    height: 200px;
    background: white;

    in-out property <length> value: 0px;
    out property <bool> anim-running: anim.running;

    anim := TweenAnimation {
        target: value;
        from: 0px;
        to: 100px;
        duration: 1000ms;
        easing: linear;
    }

    public function start_anim() {
        anim.running = true;
    }

    public function stop_anim() {
        anim.running = false;
    }

    public function restart_anim() {
        anim.running = false;
        anim.running = true;
    }
}

/*

```rust
let instance = TestCase::new().unwrap();
assert_eq!(instance.get_value(), 0.);

// Start animation
instance.invoke_start_anim();
slint_testing::mock_elapsed_time(1);

// Progress partway
slint_testing::mock_elapsed_time(250);
let value_at_250 = instance.get_value();
assert!(value_at_250 > 20. && value_at_250 < 30., "Value at 250ms should be around 25, got {}", value_at_250);
// While animating, `running` reads back as true.
assert!(instance.get_anim_running());

// Stop animation
instance.invoke_stop_anim();
slint_testing::mock_elapsed_time(1);
let stopped_value = instance.get_value();
assert_eq!(stopped_value, value_at_250);

// Restart animation from beginning
instance.invoke_restart_anim();
slint_testing::mock_elapsed_time(1);
assert_eq!(instance.get_value(), 0.);

// Let it complete
slint_testing::mock_elapsed_time(1000);
assert_eq!(instance.get_value(), 100.);
// When the animation finishes on its own, `running` must return to false.
assert!(!instance.get_anim_running());
```

```cpp
auto handle = TestCase::create();
const TestCase &instance = *handle;
assert_eq(instance.get_value(), 0.);

// Start animation
instance.invoke_start_anim();
slint_testing::mock_elapsed_time(1);

// Progress partway
slint_testing::mock_elapsed_time(250);
auto value_at_250 = instance.get_value();
assert(value_at_250 > 20. && value_at_250 < 30.);
// While animating, `running` reads back as true.
assert(instance.get_anim_running());

// Stop animation
instance.invoke_stop_anim();
slint_testing::mock_elapsed_time(1);
auto stopped_value = instance.get_value();
assert_eq(stopped_value, value_at_250);

// Restart animation from beginning
instance.invoke_restart_anim();
slint_testing::mock_elapsed_time(1);
assert_eq(instance.get_value(), 0.);

// Let it complete
slint_testing::mock_elapsed_time(1000);
assert_eq(instance.get_value(), 100.);
// When the animation finishes on its own, `running` must return to false.
assert(!instance.get_anim_running());
```

*/
