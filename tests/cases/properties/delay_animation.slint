// Copyright © SixtyFPS GmbH <info@slint.dev>
// SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

export component TestCase inherits Rectangle {
    width: 200px;
    height: 200px;
    background: white;

    out property <bool> anim-running: anim.running;

    anim := DelayAnimation {
        duration: 200ms;
    }

    public function start_anim() {
        anim.start()
    }

    public function stop_anim() {
        anim.stop()
    }

    public function restart_anim() {
        anim.restart()
    }
}

/*

```rust
let instance = TestCase::new().unwrap();
assert!(!instance.get_anim_running());

// Start the delay
instance.invoke_start_anim();
assert!(instance.get_anim_running());

// Not yet elapsed
slint_testing::mock_elapsed_time(100);
assert!(instance.get_anim_running());

// Elapsed: the delay finishes on its own
slint_testing::mock_elapsed_time(100);
assert!(!instance.get_anim_running());

// restart() begins a fresh delay
instance.invoke_restart_anim();
assert!(instance.get_anim_running());

// stop() cancels it early
instance.invoke_stop_anim();
assert!(!instance.get_anim_running());
```

```cpp
auto handle = TestCase::create();
const TestCase &instance = *handle;
assert(!instance.get_anim_running());

// Start the delay
instance.invoke_start_anim();
assert(instance.get_anim_running());

// Not yet elapsed
slint_testing::mock_elapsed_time(100);
assert(instance.get_anim_running());

// Elapsed: the delay finishes on its own
slint_testing::mock_elapsed_time(100);
assert(!instance.get_anim_running());

// restart() begins a fresh delay
instance.invoke_restart_anim();
assert(instance.get_anim_running());

// stop() cancels it early
instance.invoke_stop_anim();
assert(!instance.get_anim_running());
```

```js
var instance = new slint.TestCase({});
assert(!instance.anim_running);

// Start the delay
instance.start_anim();
assert(instance.anim_running);

// Not yet elapsed
slintlib.private_api.mock_elapsed_time(100);
assert(instance.anim_running);

// Elapsed: the delay finishes on its own
slintlib.private_api.mock_elapsed_time(100);
assert(!instance.anim_running);

// restart() begins a fresh delay
instance.restart_anim();
assert(instance.anim_running);

// stop() cancels it early
instance.stop_anim();
assert(!instance.anim_running);
```

*/
