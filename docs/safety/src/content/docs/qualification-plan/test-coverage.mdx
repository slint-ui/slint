---
title: Coverage Criteria
description: How code coverage is measured, and the level we hold it at.
---

Slint SC must support headless or offline automated testing frameworks capable of running on CI/CD pipelines to verify layout engines, event propagation, and state transitions.

Code coverage is measured with LLVM source-based coverage via `cargo-llvm-cov`.
The measured coverage of the current test suite is reported in the [Test Coverage](/qualification-report/test-coverage/) chapter of the qualification report.

## Metrics

**Addresses:** ISO 26262-6 9.4.4

ISO 26262-6:2018 Table 9 lists the structural coverage metrics at the software unit level:

| Metric | ASIL B |
| --- | --- |
| Statement coverage | Highly recommended |
| Branch coverage | Highly recommended |
| Modified Condition/Decision Coverage (MC/DC) | Recommended |

The metrics are alternative entries, so 4.3 asks for an appropriate combination of them, preferring the highly recommended ones, with a rationale that the combination meets 9.4.4.
The standard sets no percentage.
9.4.4 says only that coverage judged insufficient needs either more test cases or a rationale, and that no target value, or a low one without a rationale, counts as insufficient.

Slint SC holds the runtime at complete line, function, and region coverage, and the build fails below it.
Complete line coverage gives statement coverage.
MC/DC isn't measured, since it's only recommended at ASIL B.

:::caution[TODO]
Branch coverage isn't measured.
Complete region coverage comes close, since each arm of an `if`, `else`, or `match` is a region of its own.
It doesn't show both outcomes of an `if` without an `else`, or of the left operand of `&&` and `||`.
Either measure branch coverage, with `cargo llvm-cov --branch`, or give the rationale why region coverage is sufficient.
Check whether `--branch` needs a nightly toolchain, since that would conflict with a qualified toolchain for evidence runs.
:::

## Coverage of Slint Code

The `.slint` code of an application is measured as well, at the level of the language rather than of the generated code:
which elements, bindings, handlers and calls the tests reached, and which outcomes of every `?:`, `&&` and `||` they took.
[Coverage of Slint Code](/user-manual/slint-coverage/) describes the measurement,
and [Coverage Tool Verification](/qualification-plan/slint-coverage/) how the tool that takes it is tested.
