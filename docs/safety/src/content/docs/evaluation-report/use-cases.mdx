---
title: Use Cases
description: The use cases of Slint SC that the evaluation analyzes.
---

**Addresses:** ISO 26262-8 11.4.5.1

This chapter lists how Slint SC is used in a project.
The [potential errors](/evaluation-report/potential-errors/) and the [tool classification](/evaluation-report/tool-classification/) are derived from these use cases.

## Using Slint SC in a Project

* **ID**: UC_ADD_SLINT_SC_TO_PROJECT
* **Input**: `Cargo.toml` file in the root of a Rust project file tree
* **Output**: Modified `Cargo.toml` file that includes Slint SC as a dependency
* **Environment Constraints**: Not stated yet

When Slint SC is available, it will be a crate on [crates.io](https://crates.io/).
To add it to a project, add Slint SC as a dependency in `Cargo.toml`.

:::caution[TODO]
State the environment constraints, and show an example.
:::

## Compiling a .slint File Into Rust

* **ID**: UC_COMPILE_SLINT_FILE
* **Input**: A `.slint` file
* **Output**: Rust code that can be compiled into the final executable
* **Environment Constraints**: Not stated yet

The `slint-compiler` binary, invoked with `--slint-sc`, translates the `.slint` file into Rust code:

```sh
slint-compiler --slint-sc -o main_file.rs mainFile.slint
```

The generated code is then compiled and linked into the application together with the `slint-sc` runtime crate.

:::caution[TODO]
State the environment constraints.
`slint_build` doesn't support Slint SC yet, so there's no build-script integration.
When it gains one, this use case grows a `build.rs` variant.
:::
