[project]
name = "inertia-scroll-testing"
version = "0.1.0"
description = "System tests for the Slint inertia scroll probe."
requires-python = ">=3.10"
dependencies = [
    "pytest>=8",
    "slint-testing",
]

[tool.uv]
package = false

[tool.uv.sources]
slint-testing = { index = "slint-private" }

