# Copyright © SixtyFPS GmbH <info@slint.dev>
# SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Royalty-free-2.0 OR LicenseRef-Slint-Software-3.0

name: slint_flutter
description: A Flutter widget that renders a Slint user interface inside the widget tree.
version: 1.18.0
homepage: https://slint.dev
repository: https://github.com/slint-ui/slint
publish_to: none

environment:
  sdk: ^3.3.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  slint:
    path: ../slint

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
