# Copyright © SixtyFPS GmbH <info@slint.dev>
# SPDX-License-Identifier: MIT

name: slint
description: Slint bindings for Dart and Flutter — build the UI in .slint, the logic in Dart.
version: 1.18.0
homepage: https://slint.dev
repository: https://github.com/slint-ui/slint
publish_to: none

environment:
  sdk: ^3.3.0

dependencies:
  code_assets: ^1.2.1
  ffi: ^2.1.3
  hooks: ^2.0.2
  path: ^1.8.0

dev_dependencies:
  ffigen: ^19.0.0
  lints: ^5.0.0
  test: ^1.25.0
